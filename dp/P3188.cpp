#include <iostream>
#include <algorithm>

using namespace std;

int n, w, dp[13][50];

int main() {
    while (cin >> n >> w) {
        if (n == -1 && w == -1) {
            break;
        }

        for (int i = 0; i < n; ++i) {

        }
    }
}
